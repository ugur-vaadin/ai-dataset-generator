"""dsgen — deterministic dataset generator framework for the Vaadin AI business-case demos.
Domains live in domains/<name>/ (domain.toml, config.toml, pools/, scenarios.toml, checks.toml, lifecycle.py)."""
VERSION = "2.3.1"
