Read and follow `AGENTS.md` at the repository root. It holds the complete instructions for authoring demo
dataset packs with dsgen: the procedure to run when a user asks for a dataset, the commands, and the rules.
